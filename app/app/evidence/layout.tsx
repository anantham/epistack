import type { Metadata } from "next";

export const metadata: Metadata = { title: "Result ledger" };

export default function EvidenceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
